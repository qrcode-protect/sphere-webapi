import { test } from "@japa/runner"
import assert   from "assert";

test.group("Users create", () => {

    test("Store user", () => {

        // const user = U

        assert.equal(2 + 2, 4)
    });

})
